import { getUserData } from "./endpoints/getUserData";
import { getHealthTips } from "./endpoints/getHealthTips";
import { syncDevice } from "./endpoints/syncDevice";

export const fakeAPI = {
  getUserData,
  getHealthTips,
  syncDevice,
};
